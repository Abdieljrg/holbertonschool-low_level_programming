#include "main.h"
/**
 *_isalpha -check alpha char
 *
 * @c: the char to be check
 *
 * Return: 1 for alpha char or 0 else
 */
int _isalpha(int c)
{
	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122)
	{
return (1);
	}
return (0);
	}
