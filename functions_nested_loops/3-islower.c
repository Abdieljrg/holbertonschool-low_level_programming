#include "main.h"
/**
 *_islower -check lower case
 *
 *Return: 1 for lowercase and 0 for anything that is not lowercase
 */
int _islower(int c)
{
	if (c >= 97 && C <= 122)
	{
		return (1);
	}
	return (0);
}
